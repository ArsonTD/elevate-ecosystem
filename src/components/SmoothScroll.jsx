import { useSmoothScroll } from '../hooks/useSmoothScroll'

/** Isla sin UI: monta Lenis + ScrollTrigger (antes vivía en App.jsx). */
export default function SmoothScroll() {
  useSmoothScroll()
  return null
}
